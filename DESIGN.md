# DESIGN.md — мини-апп РХЛ

Основа — стиль **Slush** из библиотеки Refero Styles («inflatable sticker universe on pastel paper»),
[styles.refero.design/style/8b6b547f-a357-4f1b-9842-4579c62dd42b](https://styles.refero.design/style/8b6b547f-a357-4f1b-9842-4579c62dd42b).
Он переложен на мобильный спортивный продукт в Telegram. Любая правка в `webapp/` сверяется
с этим файлом. Если правило мешает — сначала меняем правило здесь.

## Северная звезда

**Стикербук болельщика.** Пастельная бумага, чёрные контуры «вырезанных ножницами» наклеек,
эмблемы клубов как стикеры, огромные надутые цифры счёта. Это не отчёт лиги, а альбом, который
хочется листать. Данные остаются точными — весёлой бывает только подача.

## Две темы

Тема следует за Telegram: `Telegram.WebApp.colorScheme` и событие `themeChanged`. Вне Telegram —
за системой (`prefers-color-scheme`). Круглая кнопка в углу шапки переключает светлую ↔ тёмную
одним нажатием и закрепляет выбор. Вернуть автоматический режим — «Я → Оформление →
Как в Telegram». Выбор хранится на устройстве. Тема ставится атрибутом `data-theme` на `<html>` до первой отрисовки,
чтобы не было вспышки белого экрана в тёмном Telegram.

**Светлая — «пастельная бумага».** Как в Slush: белое полотно, пастельные полосы, чёрные контуры.

**Тёмная — «стикеры на ночном льду», мягкий контраст.** Не инверсия светлой. Первая версия
меняла чёрное на белое и получилась резкой: белые контуры на чёрном у каждой карточки, белые
заливки кнопок, кислотные стикеры. Правила тёмной темы:
- фон — холодный графит `#131922` в тон тёмной теме Telegram, не чёрный;
- карточки на ступень светлее фона (`#1a212c`), поэтому контурам не нужно быть яркими;
- контуры тихие (`#2c3645`), текст не чисто белый (`#e4e8ee`), второстепенный — `#8e98a8`;
- стикеры — полупрозрачная заливка своего цвета, контур и светлый текст того же тона;
- сплошной яркий цвет один: синий `#4da2ff` на активной вкладке и главной кнопке. Победы и
  выбранные фильтры — тем же синим, но полупрозрачным;
- эмблемы на светлом, но не белом круге `#dde1e8`, лента и фиолетовые метки приглушены.

Шапку Telegram красим в цвет бегущей строки: чёрный в светлой теме, `#0b0f15` в тёмной.

## Цвет

Токены делятся на три вида. **Меняются местами** — текст, контуры и поверхности. **Уходят в глубокий
тон** — полосы-шапки и подсветка. **Постоянные** — стикеры, они одинаковые в обеих темах.

| Токен | Светлая | Тёмная | Роль |
| --- | --- | --- | --- |
| `--canvas` | `#ffffff` | `#131922` | Фон страницы |
| `--paper` | `#ffffff` | `#1a212c` | Карточки, листы, пилюли |
| `--ink` | `#000000` | `#e4e8ee` | Текст |
| `--muted` | `#555555` | `#8e98a8` | Второстепенный текст |
| `--edge` | `#000000` | `#2c3645` | Контуры карточек, пилюль, стикеров |
| `--edge-strong` | `#000000` | `#4a5567` | Пунктир плей-офф, ручка листа, пустые точки формы |
| `--mist` | `#e9e9e9` | `#252e3a` | Разделители внутри карточек, неактивное |
| `--act` / `--on-act` | `#000000` / `#ffffff` | `#4da2ff` / `#08121f` | Главная кнопка, активная вкладка |
| `--sel` | `#dceeff` | `#1f2c3f` | Строка своей команды, выбранная карточка, нажатие |
| `--band-sky` | `#dceeff` | `#182538` | Полоса «Главной» и заставка |
| `--band-lavender` | `#e9ccff` | `#211d33` | Полоса «Календаря» |
| `--band-mint` | `#55db9c` | `#15291f` | Полоса «Таблицы» |
| `--band-concrete` | `#cccccc` | `#1c222c` | Полоса «Команды» |
| `--lead-home` / `--lead-away` | `--mint` / `--lavender` | 30% / 34% тех же цветов | Заливка «кто ведёт» в ходе матча |
| `--home-*` / `--away-*` / `--hot-*` | сплошная заливка, чёрный текст и контур | заливка 12–18%, текст и контур того же тона | Стикеры «Дома» / «Выезд» / «Сегодня» |

Постоянные цвета стикеров:

| Токен | Hex | Роль |
| --- | --- | --- |
| `--sticker` | `#ffffff` | Круг под эмблемой, текст на фиолетовом |
| `--on-sticker` | `#000000` | Текст на мятном, лавандовом и жёлтом |
| `--blue` | `#4da2ff` | Фирменная «надувная лента» и декор. Не кнопка и не ссылка |
| `--mint` | `#55db9c` | Стикер «Дома», метки «бол / мен» |
| `--lavender` | `#e9ccff` | Стикер «Выезд», метки периодов, стикер «плей-офф» |
| `--ember` | `#fb4903` | Стикер «Сегодня». Текст на нём чёрный: белый даёт 3.5:1, ниже AA |
| `--sun` | `#ffd731` | Декор: звёздочки в бегущей строке. Никогда под текст |
| `--violet` | `#5c4ade` | Метки-аббревиатуры команд без эмблемы |

Бегущая строка в обеих темах чёрная с белым текстом.

Палитра стикеров общая: на одном экране живут несколько цветов, ни один не «главный акцент».

**Победа и поражение — не цветом, а заливкой.** Победа — залитая пилюля цвета `--ink` («В»),
поражение — пилюля с контуром («П»). В форме: залитый кружок — победа, пустой — поражение.
Так работает и без цвета, и в обеих темах, и в духе Slush, где зелёный — наклейка, а не статус.

## Экран запуска

Заставка — «наклейки шлёпаются на лист». Около полутора секунд, один жест, без крутилки.

**Раскадровка**
- 0–500 мс: буквы Р, Х, Л по очереди шлёпаются на лист с отскоком (0 / 70 / 140 мс, по 360 мс).
  Каждая буква — вырезанная наклейка: чёрная буква, белая кайма, тонкий чёрный край. Буквы — векторные
  контуры Unbounded 800, поэтому заставка не ждёт загрузки шрифта
- 0–600 мс: за буквами прорисовывается синяя надувная лента
- 250–430 мс: вокруг выскакивают стикеры — шайба, звезда `--sun`, «сезон 26/27» `--mint`,
  эмблема любимой команды (из прошлого запуска)
- Уход: лист за 380 мс уезжает вверх под чёрную бегущую строку. Начинается, когда данные готовы
  и прошло не меньше 1,4 с: буквы приземляются к 500 мс, ещё почти секунду болельщик видит
  готовую наклейку целиком. Это фирменный момент первого знакомства. Дольше не держим
- Повторный запуск, когда матчи уже на устройстве, — не меньше 600 мс: буквы успевают
  приземлиться, и сразу свой экран. Болельщик открывает приложение проверить счёт, а не смотреть
  заставку в третий раз
- Если через 2,2 с данных всё ещё нет — снизу выезжает чёрная полоса «Загружаем матчи ✦»
- `prefers-reduced-motion`: без анимации, сразу финальный кадр

Фон — `--band-sky` светлой или тёмной темы, стикеры одинаковые в обеих.

**Скорость — важнее анимации**
- Разметка и стили заставки встроены в `index.html`: она рисуется, пока грузится всё остальное
- Скрипт Telegram подключается асинхронно и не держит запуск. Тему до его прихода берём
  из `tgWebAppThemeParams` в адресе страницы
- `league.json` начинает качаться сразу (`<link rel="preload">`), параллельно со скриптами
- Данные хранятся на устройстве: повторный запуск рисуется из них мгновенно,
  свежие подтягиваются в фоне и тихо перерисовывают экран

**Нативный экран загрузки Telegram** показывается ещё до страницы. Настраивается в @BotFather
для главного мини-аппа бота: иконка `webapp/brand/icon-512.png` (или `icon.svg`), фон светлой
темы `#dceeff`, тёмной `#182538` — тот же, что у заставки, поэтому переход незаметен.

## Типографика

- **Unbounded 800** — дисплей: название команды в шапке, счёт, крупные числа статистики,
  дата в календаре. Межстрочный 0.85–0.9. Надутые широкие буквы с кириллицей — наша замена Lateral
- **Onest 500 / 700** — весь интерфейс. 500 для текста и метаданных, 700 для названий команд,
  кнопок и меток. Кнопки и прописные метки — трекинг +0.03em
- Ровные цифры `"tnum"` — только у чисел

| Роль | Шрифт / размер / начертание / межстрочный |
| --- | --- |
| Название команды в шапке | Unbounded 18–42 / 800 / 0.9, кегль по самому длинному слову |
| Счёт на табло | Unbounded 40–52 / 800 / 0.85, на узком экране меньше |
| Число в статистике | Unbounded 26 / 800 / 1 |
| Дата в строке календаря | Unbounded 18 / 800 / 1 |
| Заголовок экрана | Unbounded 28 / 800 / 0.95 |
| Название команды | Onest 16 / 700 / 1.2 |
| Текст | Onest 15 / 500 / 1.4 |
| Подпись | Onest 13 / 500 / 1.35 |
| Метка секции | Onest 12 / 700 / прописные / +0.04em |

## Формы и отступы

- Контур 1px `--ink` у всего, что можно нажать: карточек, кнопок, пилюль, стикеров.
  Внутри строк списка контуров нет: мини-эмблема 24px обведена тихим `--mist`, дата — без рамки.
  Рамка в строке — только у особенного (дата ближайшего матча). Иначе список превращается
  в сетку из десятков чёрных колечек
- Радиусы: карточки 20, крупные табло 28, пилюли и кнопки 9999, стикеры эмблем — круг
- Ни теней, ни градиентов. Глубину дают цветные полосы и контуры
- Шкала 4 / 8 / 12 / 16 / 20 / 24 / 32. Поля экрана 16, между секциями 24
- Каждый экран начинается с цветной полосы-шапки во всю ширину: Главная — `--band-sky`,
  Календарь — `--band-lavender`, Таблица — `--band-mint`, Я — `--band-concrete`
- Если в полосе есть фильтры списка, заголовок при прокрутке уезжает, а фильтры прилипают
  под бегущей строкой на той же полосе. Высота полосы не меняется от выбранного фильтра:
  нажатие не должно сдвигать список

## Компоненты

**Бегущая строка.** Чёрная полоса на всю ширину вверху, белый Onest 700 12px прописными:
«РХЛ 2026/27 · старт 3 октября · 26 команд · …». Единственное постоянное движение в продукте,
поэтому оно замирает там, где читают цифры: на экране «Таблица» и под открытой карточкой матча.
При `prefers-reduced-motion` стоит на месте.

**Стикер эмблемы.** Белый круг `--sticker` в обеих темах, контур 1px `--ink`, эмблема внутри с отступом 12%. В шапке —
72px и повёрнут на −6°. Если эмблемы нет — круг `--violet` с аббревиатурой белым Onest 700.

**Надувная лента.** Плоская SVG-лента `--blue` с чёрным контуром за стикером в шапке «Главной».
Декор, никогда не несёт смысла.

**Табло матча.** Белая карточка, радиус 28, контур. Две эмблемы-стикера по краям, между ними
счёт Unbounded 40–52. Под счётом — «овертайм» или «буллиты»; победа в основное время без
подписи. Названия команд переносятся только между словами и по дефису. Сверху — стикеры «Дома» / «Выезд» / «Сегодня». Снизу — периоды в пилюлях
с контуром.

**Очные встречи** (карточка матча, ADR-006). Белая карточка, радиус 20, контур. Сверху мини-эмблемы
по краям и победы каждой стороны Unbounded 26, между ними метка «Победы». Под ними полоса 10px
в контуре: доля побед хозяев залита `--act` — исход заливкой, как у пилюли «В». Ниже шайбы
Unbounded 18. Под тонкой линией `--mist` — вывод одной строкой Onest 15: «По истории встреч
сильнее …», никогда «фаворит» — прогноз мы не даём. Дальше «Последние встречи» — строки
«Вся лига»: победитель жирным, справа ОТ/Б пилюлей и дата `--muted`. Встреча с протоколом
нажимается и открывает её разбор (ADR-008) в том же листе, в шапке — круглая «Назад» слева.
Встреч нет — пустое состояние «В НМХЛ и РХЛ раньше не встречались».

**Строка матча.** Два вида — смотря чей это календарь.
- *Календарь одной команды* (свой, чужой, «Дальше» на Главной). Своя команда уже названа
  в фильтре, в строке её нет. Слева дата 44×44 без рамки: число Unbounded 18, месяц и день
  недели 10px прописными. Дальше эмблема и **соперник** Onest 700, под ним мини-стикер
  «Дома» (`--mint`) или «Выезд» (`--lavender`) высотой 18 без контура и город выезда `--muted`.
  Справа — счёт и пилюля исхода или время начала. Строка около 64px
- *Вся лига.* Матчи сгруппированы по дню: подзаголовок «Сб, 3 октября» внутри карточки,
  под ним строки без даты — две строки команд с мини-эмблемами и голами, своя команда жирным.

Ближайший матч отмечен датой-стикером с контуром 1px `--ink` (в лиге — подзаголовком дня
с контуром) — единственная рамка в списке. Прошедшие даты — `--muted`. Колонка исхода —
по содержимому, чтобы название команды помещалось целиком и на экране 320px.

**Переключатель темы.** Круглый стикер 40px в правом верхнем углу цветной шапки на каждом
экране, фон `--paper`, контур `--edge-strong`, повёрнут на −6° как эмблема в шапке. В светлой
теме — луна `--lavender`, в тёмной — солнце `--sun`: иконка показывает, куда переключит.
Цвета переходят за 0.3 с только по нажатию. При запуске перехода нет, чтобы экран не мигал.
Заголовок шапки не заходит под кнопку.

**Название в шапке «Главной».** Слово никогда не рвётся посередине. Кегль считается от самого
длинного слова названия (кусок до дефиса считается отдельно): «Рязань-ВДВ» — 42, «Калужские
Ракеты» и «Красная машина Академия» — меньше, но целыми словами.

**Пилюли-фильтры.** Белые, контур 1px, Onest 700 13px, высота 40. Активная — чёрная с белым текстом.

**Переключатель-сегменты.** Главный фильтр экрана, когда вариантов 2–3 и они взаимоисключающие:
одна пилюля во всю ширину с контуром, внутри равные сегменты, активный залит `--act`.
Заливка — отдельный «бегунок»: при выборе он перетекает к новому сегменту, а не появляется на нём.
Длинный текст сегмента — с многоточием, ряд никогда не вылезает за край. В Календаре:
«Моя команда · Вся лига · Другая ▾». «Другая» открывает лист выбора команды, после выбора
в сегменте название клуба.

**Вторичный фильтр.** Уточнение под сегментами («Все · Дома · Выезд», «Все · Восток · Запад»):
чипы 32px без контура и заливки, активный — пилюля `--paper` с контуром `--edge`, тоже бегунком. Зона
нажатия дотянута до 40px. Чёрной заливки нет: на экране одна чёрная кнопка-фильтр, не две.

**Главная кнопка.** Пилюля `--act` с текстом `--on-act` (в светлой теме чёрная, в тёмной синяя), Onest 700 15px, трекинг +0.03em. Одна на экран.
Показываем, только когда есть что сделать. Если нечего — кнопки нет, серой она не висит.
На экране «Я» главная кнопка — «Выложить в историю», рядом вторичная «Позвать болеть вместе»:
пилюля `--paper` с контуром `--edge`.

**Выбор команды.** Карточки в две колонки, на экране уже 360px — в одну. Название — до двух строк,
чтобы «Динамо-576» и «Динамо-Карелия» не превращались в одинаковое «Динам…». При первом запуске —
отдельный экран с кнопкой «Готово». Потом — **лист смены команды**, как карточка матча: открывается
со строки «Сменить команду» на экране «Я» и по нажатию на эмблему в шапке «Главной». Одно касание
по клубу — выбор, лист закрывается. Тот же лист с заголовком «Календарь команды» открывает
сегмент «Другая ▾» в Календаре: выбор показывает календарь клуба, любимая команда не меняется.

**Паспорт болельщика** (экран «Я», ADR-004). Карточка-стикер цвета `--violet` с белым текстом,
радиус 28, контур — как карточка скачивания у Slush. Сверху белая пилюля «Паспорт болельщика» и
звезда `--sun`. Эмблема 76px, повёрнута на −6°, рядом «Болею за» и название клуба Unbounded,
кегль по самому длинному слову. Ниже имя из Telegram и сезон, внизу белая панель с тремя цифрами
сезона — теми же, что на «Главной». В тёмной теме заливка `#272650`, текст `#dcd6ff`, контур
фиолетовый полупрозрачный.

**Строки настроек.** Белая карточка с контуром, строки от 60px: иконка 20px, название Onest 700,
пояснение 12px `--muted`, шеврон. Недоступное пока — серым, без шеврона, с честным «скоро».

**Карточка для истории.** `webapp/story.html` → `webapp/stories/<id>.jpg`, 1080×1920. Пастельный
`--band-sky`, чёрная полоса наискосок, надувная лента за эмблемой 220px, стикеры шайбы и звезды,
мятная пилюля «Болею за», название Unbounded до 64px, внизу белая плашка «Матчи, таблица и счёт
РХЛ». Верх и низ кадра закрывает интерфейс Stories — всё важное между ними. Без имени и цифр:
картинка общая для всех болельщиков клуба и не устаревает.

**Таблица.** Белая карточка с контуром. Строки 48px, разделители `--mist`. Позиция Unbounded 14,
эмблема 24px, очки Onest 700. Своя команда — фон `--sel`. После 8-го места — пунктир `--ink`
и стикер «плей-офф». Название команды — до двух строк, без многоточия. На экране уже 360px
колонки «В» и «П» прячутся: остаются игры, шайбы и очки.

**Лидеры лиги** (ADR-009). Правило одно: в блоке — один сильный объект, остальное тихое.
- *Шапка «Таблицы»* — заголовок и сегменты «Команды · Игроки», больше ничего. Высота шапки
  одинаковая в обоих видах
- *Конференции* — вторичный фильтр, лёгкие чипы «Восток · Запад» под шапкой во всю ширину,
  с бегунком, как в Календаре
- *Игроки* — над сеткой меткой чей сезон («НМХЛ 2025/26 · прошлый сезон»). Сетка 2×3 карточек
  показателей: белая карточка, радиус 20, контур. Сверху метка показателя прописными `--muted`,
  главное — число лидера Unbounded 34, под ним подпись («очков», «% отражённых»), внизу
  **фамилия** и имя в одну строку и клуб `--muted`. Эмблема клуба лидера — стикер 36px,
  наклеен на правый верхний угол карточки чуть за край и повёрнут на −8°. Клуба нет в РХЛ —
  без стикера. Карточка своей команды — фон `--sel`. Нажатие — топ-10
- *Топ-10* — лист снизу, как карточка матча: заголовок показателя Unbounded 26 и «топ-10»,
  строки списка: место Unbounded 14, эмблема 24, игрок Onest 700 15 и подпись «клуб · цифры»
  `--muted`, справа одно число Unbounded 18. Никаких колонок. Своя команда — `--sel`; своих нет в
  десятке — пунктир, стикер «лучший в команде» и её лучший игрок
- *«<Команда> в лидерах»* — под сеткой, по строке на игрока: лучшее место Unbounded, имя,
  подписью все места («3-й по плюс-минусу · 10-й по очкам»). Нажатие открывает список, где он
  выше всего. Нет никого в тридцатке — блока нет

**Нижняя навигация.** Парящая белая пилюля с контуром, отступ 12px от краёв и низа.
Активная вкладка — чёрная пилюля с иконкой и подписью белым, неактивные — только иконка.
Активная шире соседних ровно настолько, чтобы влезла подпись, — не самое крупное пятно на экране.
Чёрная пилюля — бегунок: при смене вкладки перетекает к новой, иконки съезжают на новые места,
подпись проявляется. Ширина вкладок меняется сразу — плавность дают бегунок и иконки.
Вкладки: «Главная», «Календарь», «Таблица», «Я».

**Карточка матча.** Лист снизу, белый, радиус 28 сверху, контур сверху. Ручка сверху — настоящая:
лист тянется пальцем вниз и закрывается. Голы по периодам:
период — стикер-метка, время Onest 700, мини-эмблема забившей команды, автор, передачи серым `#555`.

**Разбор сыгранного матча** (ADR-008). Под табло, сверху вниз — от ответа к подробностям:
- *Сюжет* — карточка `--band-sky`, радиус 20, контур, звёздочка `--sun` слева, одна-две фразы
  Onest 15. Нет фразы — нет карточки;
- *Ход матча* — карточка радиус 20. Ступенчатая линия разницы в счёте 2px `--ink` по минутам,
  выше оси залито `--lead-home` (ведут хозяева), ниже — `--lead-away` (гости): цвета стикеров
  «Дома» и «Выезд». Шкала — по данным, не меньше ±2. Точка гола 9px: пустая с контуром, в
  большинстве — залитая `--ink`, выбранная — толще. Под графиком две дорожки удалений, полосы
  `--muted` 6px. Периоды — колонки во всю высоту: второй и овертайм подложены `--mist` 50%,
  границы сплошные, внизу колонки «1-й период» прописными `--ink` 10px. Шкала 9px `--muted`. Ниже — выбранный гол строкой и легенда. Зона нажатия
  точки 28px;
- *Сегменты «Голы · Статистика · Составы»* прилипают к верху листа. Вкладки без данных не
  показываются;
- *Статистика* — строки «хозяева · метка · гости», цифры Unbounded 18, под ними две пилюли 10px:
  у кого больше — залита `--act` (у штрафа — у кого меньше). Исход заливкой, как у очных встреч.
  Вратари — строки с процентом Unbounded 18;
- *Составы* — вторичный фильтр с командами, группы «Вратари · Защитники · Нападающие», номер
  Unbounded 13 `--muted`, «К»/«А» серой меткой, очки справа. Кто набрал очки — строка `--sel`.

**Доступность.** Всё, что нажимается, — кнопка или `role="button"` с `tabindex`, Enter и пробел
работают. Фокус с клавиатуры — контур 2px `--blue` (это не заливка кнопки, правило про синий не
нарушает). Карточка матча забирает фокус на крестик, Esc закрывает её и возвращает фокус.
Цель нажатия — не меньше 40px. Текст — не меньше 10px и контраст не ниже 4.5:1.

## Движение

Движение объясняет, откуда что взялось и куда делось, и отвечает на касание. Украшением оно
бывает в двух местах: на заставке и один раз при первом показе графика «Ход матча».

**Длительности и кривые**
- Отклик на касание — 120 мс: пилюли, кнопки и карточки чуть вдавливаются (`scale(.97)`),
  строки списков и таблицы подсвечиваются `--sel`
- Переходы — 180–280 мс, торможение `cubic-bezier(.2, .8, .2, 1)`. Уход — быстрее прихода
  и с разгоном `cubic-bezier(.4, 0, 1, 1)`
- «Вау» — 400–600 мс, и только при первом показе, не при каждом нажатии
- Двигаем только `transform` и `opacity` — их ведёт видеокарта, и движение не замирает, пока
  процессор перерисовывает экран. Ширину не анимируем: бегунок собран из двух полукругов
  и середины, ширина — масштабом середины
- Сначала движение, потом работа: при смене вкладки бегунок трогается в том же кадре,
  а новый экран собирается в следующем
- В прокручиваемых списках подсветка нажатия появляется через 60 мс: если палец начал
  прокрутку, строки не мигают
- Эмблемы декодируются заранее, чтобы при перерисовке не мигать пустыми кругами
- `prefers-reduced-motion`: всё мгновенно, конечные состояния те же

**Что движется**
- *Смена экрана* — новый экран проявляется со сдвигом 16px со стороны вкладки, на которую
  нажали: правее — справа, левее — слева
- *Выбранное* перетекает, а не перескакивает: бегунок в меню, сегментах и чипах.
  Выбранная карточка клуба коротко пружинит, эмблема на ней поворачивается на −6°, как в шапке.
  Кнопка «Готово» выезжает снизу после первого выбора
- *Карточка матча* поднимается снизу за 220 мс и уходит вниз за 200 мс. Переход в прошлую
  встречу — содержимое въезжает справа, «Назад» — слева. Лист закрывается свайпом вниз: дальше
  30% высоты или быстрым движением — закрыть, иначе пружиной на место
- *Смена темы* — новая тема раскрывается кругом от нажатой кнопки за 420 мс
- *Ход матча* при первом показе рисуется слева направо за 600 мс, как лента на заставке,
  точки голов выскакивают по порядку
- *Цифры сезона на «Главной»* при первом показе за день досчитывают до значения за 400 мс

**Загрузка без прыжков.** Что догружается в карточку матча (очные встречи, разбор), сразу
занимает место скелетоном — плашками `--mist` примерно той же высоты, они тихо пульсируют.
Данные пришли — проявляются за 150 мс. Не пришли — «Не удалось загрузить» и кнопка «Повторить».
Нажатие отвечает раньше тяжёлой работы: фильтр сначала выбирается, потом перерисовывается
список. После смены фильтра календарь остаётся на том же дне.

**Наведение мыши** (Telegram Desktop и веб) — подсветка `--sel` у строк, карточек и пилюль.
Только там, где есть мышь (`hover: hover`), чтобы на телефоне подсветка не залипала.

## Эмблемы

`webapp/logos/` — эмблемы 19 клубов. Источники: официальный сайт лиги `nmhl.fhr.ru` и, где
там картинка мельче, r-hockey.ru (200×200). Путь к эмблеме — поле `logo` в `teams.json`.
У «Витязь-Подольска», «Ахмат-Гранита», «Калужских Ракет», «Красной машины», «Динамо-Карелии»,
«Ростова» и «Краснодара» эмблем в хорошем качестве пока нет — показываем стикер с аббревиатурой.
Эмблемы принадлежат клубам; мы используем их только чтобы обозначить команду.

## Правила

**Делай**
- Контур 1px `--ink` у всех карточек, кнопок и стикеров — но не у мелочей внутри строк списка
- Несколько цветов палитры на экране — стикерами и полосами
- Огромный Unbounded там, где число или имя — главное на экране
- Исход матча — заливкой, а не цветом
- Всё, что пришло из данных, вставляем через `esc()`

**Не делай**
- Тени и градиенты — нигде
- Радиус карточек меньше 16 и не-пилюльные кнопки
- `--blue` как кнопку или ссылку
- Зелёный и красный как «победа / поражение»
- `--sun` под текстом
- Эмодзи в интерфейсе — только SVG-иконки и стикеры
- Нативный `<select>`: в iOS он синий, шириной с самый длинный вариант и не похож на пилюлю.
  Выбор из списка — листом
- Повторять в каждой строке то, что уже сказано фильтром
