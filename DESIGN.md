# DESIGN.md — мини-апп РХЛ

Основа — дизайн-система **Linear** из библиотеки [Refero Styles](https://styles.refero.design/style/90ce5883-bb24-4466-93f7-801cd617b0d1)
(«midnight precision instrument»), переложенная на спортивный продукт. Любая правка интерфейса
в `webapp/` сверяется с этим файлом. Если правило мешает — сначала меняем правило здесь.

## Северная звезда

**Табло ночного катка.** Тёмный лёд, белые цифры, одна лаймовая подсветка. Интерфейс — точный
прибор: цифры ровные, линии тонкие, украшений нет. Единственная текстура экрана — сами данные:
счёт, календарь, таблица.

Почему Linear: у нас плотные данные (624 матча, 26 команд, таблицы), а Linear сделан для
плотных данных. Тёмное полотно не слепит на трибуне вечером. Один акцент не спорит
с цветами клубов.

Тема всегда тёмная, независимо от темы Telegram. Шапку и фон Telegram красим в `#08090a`
через `setHeaderColor` и `setBackgroundColor`.

## Цвет

| Токен | Hex | Роль |
| --- | --- | --- |
| `--void` | `#08090a` | Полотно страницы |
| `--carbon` | `#0f1011` | Карточки, нижняя навигация |
| `--obsidian` | `#161718` | Поднятые панели, карточка матча, строка своей команды |
| `--graphite` | `#23252a` | Рамки-волоски, разделители |
| `--smoke` | `#383b3f` | Рамки контрастнее, линия плей-офф |
| `--ash` | `#62666d` | Неактивные иконки, третичные подписи |
| `--fog` | `#8a8f98` | Подписи, метаданные |
| `--mist` | `#d0d6e0` | Основной текст |
| `--paper` | `#ffffff` | Заголовки, счёт, своя команда |
| `--lime` | `#e4f222` | Один главный акцент (см. правила) |
| `--win` | `#27a644` | Победа — только в бейджах и точках формы |
| `--loss` | `#eb5757` | Поражение — только в бейджах и точках формы |

### Где можно лайм

- Индикатор активной вкладки внизу
- Единственная главная кнопка на экране («Готово» при выборе команды)
- Отметка «сегодня» и «ближайший матч» в календаре — это «активное» состояние, как в навигации

Больше нигде. Лаймом не красят текст, фон карточек, свою команду и победы.

## Типографика

- **Inter** (variable) с `font-feature-settings: "cv01", "ss03", "zero"` — всё, от вкладок до счёта.
  У чисел (счёт, очки, таблица, даты) добавляется `"tnum"`. В обычном тексте его нет:
  у Inter `tnum` расширяет дефис, и «Рязань-ВДВ» превращается в «Рязань - ВДВ»
- **JetBrains Mono** — только технические метаданные: номер матча, игровое время гола,
  аббревиатуры команд, позиция в таблице, даты в строках календаря
- Начертания: 400 и 510, для акцента 590. Жирнее 590 не используем

| Роль | Размер / начертание / межстрочный / трекинг |
| --- | --- |
| Счёт в карточке | 48 / 510 / 1.0 / −0.022em |
| Заголовок экрана | 28 / 510 / 1.1 / −0.02em |
| Название команды крупно | 17 / 510 / 1.25 / −0.011em |
| Текст | 15 / 400 / 1.5 / −0.011em |
| Подпись | 13 / 400 / 1.35 |
| Метка секции | 12 / 510 / 1.3, прописные, +0.04em, `--fog` |
| Микро | 11 / 510 |

## Отступы и формы

- Шкала 4 / 8 / 12 / 16 / 24 / 32. Между элементами 8, внутри карточки 16, между секциями 24–32
  (мобильная версия лестницы Linear 8/12/24/96)
- Радиусы: карточки 12, кнопки и поля 6, бейджи 4, пилюли 9999. Больше 12 не бывает
- Разделение поверхностей — рамкой-волоском `inset 0 0 0 1px var(--graphite)`, не тенью.
  Тень только у всплывающей карточки матча
- Поля экрана 16, максимальная ширина 560

## Компоненты

**Карточка.** `--carbon`, радиус 12, волосок `--graphite`, padding 16.

**Табло матча.** Карточка с двумя колонками команд и счётом по центру: 48/510, `--paper`.
Под счётом — ОТ/Б микрошрифтом `--fog`, ниже — периоды моношрифтом в пилюлях.
До матча вместо счёта — время начала или «—».

**Метка команды.** Квадрат 32×32 (в списках 24×24), радиус 6, `--obsidian`, волосок `--graphite`,
аббревиатура моношрифтом 10/510 `--mist`. Логотипов клубов у нас нет — метка их заменяет.
Своя команда: текст `--paper`, метка с волоском `--smoke`.

**Строка матча.** Колонка даты моношрифтом (`03` крупно, `ОКТ СБ` мелко), две строки команд с метками,
справа счёт 15/510 и бейдж исхода «В»/«П» для своей команды.

**Бейдж.** Радиус 4, padding 0 6, 11/510. Нейтральный: `rgba(255,255,255,.05)` + `--fog`.
Победа: `rgba(39,166,68,.14)` + `#4cc26a`. Поражение: `rgba(235,87,87,.14)` + `#f07878`.
«Предварительно»: нейтральный.

**Пилюли-фильтры.** Сегментированная группа на `--carbon` с волоском. Активная — `--obsidian`,
текст `--paper`. Не лайм: лайм занят навигацией.

**Таблица.** Плотные строки 44px, позиция моношрифтом `--ash`, метка + название, цифры `tnum`.
Очки — `--paper` 510. Своя команда — фон `--obsidian`. После 8-го места — линия `--smoke`
и подпись «Плей-офф» микрошрифтом.

**Форма.** Пять точек 6×6: `--win` / `--loss`.

**Нижняя навигация.** `--carbon` с волоском сверху. Иконки — линейные SVG 22px, неактивные
`--ash`, активная `--paper` с лаймовой чертой 16×2 над иконкой. Подписи 11/510.

**Карточка матча.** Лист снизу на `--void`, радиус сверху 12, тень `0 -8px 32px rgba(0,0,0,.5)`.
Голы — лента: время моношрифтом, автор `--mist`, передачи `--fog`, счёт после гола справа.
Голы гостей выравниваются вправо.

## Правила

**Делай**
- Ровные цифры (`tnum`) у всех чисел: счёт, очки, шайбы, даты. Только у чисел
- Один фокус на экран: на главной — табло следующего матча
- Иконки — линейные SVG одним цветом. Эмодзи в интерфейсе не используем
- Неофициальные данные подписываем бейджем «предварительно», а не прячем
- Всё, что пришло из данных, вставляем через `esc()`

**Не делай**
- Жирнее 590
- Градиенты на кнопках, карточках и тексте
- Радиусы больше 12
- Тени для разделения карточек
- Цветной основной текст: текст только в шкале `--paper` / `--mist` / `--fog` / `--ash`
- Второй хроматический акцент для действий
